<h1><%= @post.title %></h1>
<p>by <%= link_to @post.author.name, author_path(@post.author) if @post.author %> <br /><br>
<button><%= link_to "Edit Post", edit_author_post_path(@post.author, @post) if @post.author %></button></p>
<p><%= @post.description %> </p>
